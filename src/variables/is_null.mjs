/**
 * Проверяет, равно ли значение переменной `null`
 *
 * @param {any} value Переменная для проверки.
 * @returns {boolean} Возвращает `true`, если значение `value` — `null`, иначе `false`.
 */
export default function is_null(value) {
    return value === null;
}
